function FindProxyForURL(url, host) {
    // Цей PAC файл налаштовує проксі для всіх URL.
    // Якщо потрібно, додайте умови, наприклад, для певних доменів.
    // Для аутентифікації (логін і пароль) iOS запитає їх окремо, або використовуйте конфігураційний профіль.
    return "PROXY rsg-31932.sp4.ovh:11001; DIRECT";
}
